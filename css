/* تغيير لون خلفية الصفحة *//* style.css */
body {
    background-color: #f0f0f0; /* لون خلفية الصفحة */
    font-family: Arial, sans-serif; /* نوع الخط المستخدم في الصفحة */
}

h1 {
    color: #333; /* لون النص للعناوين */
    text-align: center; /* محاذاة النص إلى المنتصف */
}

p {
    color: #666; /* لون النص للفقرات */
    font-size: 18px; /* حجم الخط للفقرات */
    line-height: 1.6; /* المسافة بين الأسطر في الفقرات */
    margin: 20px; /* المسافة حول الفقرات */
}